# actualizaciones
en la carpeta `backend/database/models` estan los modelos que se usaran a futuro y tambien en este caso el modelo de los usuarios.
en `router/users.py` estan los POST/GET que se podran usar para obtener info.
asi mismo en el services user estan las funciones corresponientes

